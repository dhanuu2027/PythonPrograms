# Problem: Largest and Smallest element
# Input: n=5, array=[64, 34, 25, 12, 22]
# Output: Largest: 64, Smallest: 12n=int(input())
a=list(map(int,input().split()))
min=a[0]
max=a[0]
for i in range(n):
    if a[i]<min:
        min=a[i]
    if a[i]>max:
        max=a[i]
print(f"Smallest: {min}")
print(f"Largest: {max}")
