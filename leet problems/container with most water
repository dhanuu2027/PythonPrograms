height=list(map(int,input().split()))
left=0
right=len(height)-1
m=0
while left<right:
    width=right-left
    h=min(height[left],height[right])
    area=width*h
    m=max(area,m)
    if height[left]<height[right]:
        left+=1
    else:
        right-=1
print(m)
